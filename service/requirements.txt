# Core service — training-free lanes (B, C) + quality gate + judge.
# Deliberately small so it installs fast and runs on free CPU hosting
# (HuggingFace Spaces free tier: CPU, 16GB).
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.5
pillow==11.1.0
numpy==2.2.1
opencv-python-headless==4.11.0.86
scipy==1.15.1
httpx==0.28.1
pynacl==1.5.0

# Lane A (trained) and Lane E (face match) add torch/timm/insightface.
# Kept in requirements-ml.txt so the base service stays deployable
# without them — see service/README.md.
